{
  "name": "thronefall-mobile-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mobile client (TypeScript + Vite + three.js) — a thin communication/rendering layer over the authoritative server. See docs/GAME_DESIGN.md §5.2.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
